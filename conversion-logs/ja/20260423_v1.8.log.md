# JA v1.8 변환 로그

| 항목 | 값 |
|------|-----|
| 원본 DOCX | `~/Downloads/20260410_메가존클라우드 HyeprBilling 개인정보처리방침_v1.8 개정(안)(일문).docx` |
| 산출 JSON | `misc/privacy/ja_20260423_v1.8.json` |
| 변환 일시 | 2026-04-23 |
| 에디터 커밋 | `88ea6a4` |
| 스킬 | `privacy-convert@3.1.0` |

## [INFO]
- headers: 1
- sections: 14
- tables: 11

## [AUTO-FIX — 스크립트 자동 반영]

| 원본(DOCX) | 최종(JSON) |
|-----------|-----------|
| 헤딩 서식 혼재 — 다수 13개 bold, 소수 1개 non-bold (`第10条（お客様の権利・義務およびその行使方法）`) | 일관 포맷(내보내기 시 자동 정상화) |
| 번호 공백 `1 1` (`第1 1条…`) | `11` (`第11条（個人情報権利保障）`) |

## [REVIEW — 결정 반영]

| 항목 | 원본(DOCX) | 결정 | 최종(JSON) |
|------|-----------|------|-----------|
| 번호 스타일 혼재 | `1.` 26건 / `①②③` 9건 / `ⅰ)` 4건 / `1)` 2건 | 유지 | 동일 |
| 도트 문자 (제7조 L2) | `樹立·施行` (`·` 1개, 전체 분포 `・` 18 / `·` 1) | `·` → `・` 통일 | `樹立・施行` |
| 취소선 — 제4조 L3 표 헤더 | `<th><s>委託業務の内容</s></th>` | 옵션 2: 태그만 벗기고 텍스트 유지 (표 헤더 보존) | `<th>委託業務の内容</th>` |
| 취소선 — 제4조 L6 표 헤더 | `<th><s>再受託会社</s></th>` | 옵션 2: 태그만 벗기고 텍스트 유지 (표 헤더 보존) | `<th>再受託会社</th>` |
| 취소선 — 제12조 L2 주소 | `(ビッカラムドン301-2)<s>3階</s>個人情報侵害申告センター` | 옵션 1: 태그+내용 삭제 | `(ビッカラムドン301-2)個人情報侵害申告センター` |

## [MANUAL — 스크립트 휴리스틱 누락분 수동 반영]

| 위치 | 원본(스크립트 파싱 결과) | 최종(JSON) |
|------|----------------------|-----------|
| 제3조 L6 (법령에 의한 보유기간 표) | `<tbody><tr><td>区分</td><td>関連法令</td><td>利用期間<br>（保有期間）</td></tr>…` (첫 행이 일반 tr, 헤더 셀 내부에 `<br>` 유지) | `<thead><tr class='table-secondary'><th>区分</th><th>関連法令</th><th>利用期間（保有期間）</th></tr></thead><tbody>…` (첫 행 thead 승격 + 헤더 셀 내 `<br>` 제거) |
| 제14조 L1 (문장 끝 마침표) | `…以前のバージョンの個人情報処理方針をご確認いただけます` (구두점 `。` 누락) | `…以前のバージョンの個人情報処理方針をご確認いただけます。` |
| 제6조 L1~L2 (br-fix 부작용 복원) | 원본 단락 내 `<w:br/>` 로 시각 정렬되어 있던 한 문장이 `88ea6a4` 커밋 이후 2줄로 분리됨: `…個人情報が不要となったときは、` + `当該情報を遅滞なく破棄いたします。` | 의미상 한 문장이므로 1줄 병합: `…個人情報が不要となったときは、当該情報を遅滞なく破棄いたします。` (제6조 L3 이하의 번호 목록 `1)`/`2)` 분리는 유지) |

## [EXTERNAL — 외부 입력]

해당 없음. 원본이 이미 V1.8 / 2026년 4월 23일 로 확정 기재되어 있음.

## [BR-SPLIT — 줄바꿈 버그 수정분 (커밋 `88ea6a4`)]

| 위치 | 원본 DOCX (본문 단락 내 `<w:br/>`) | 최종(JSON) |
|------|-----------------------------|-----------|
| 제6조 L3 | 1개 단락에 `不要となったときは、` + `<w:br/>` + `当該情報を遅滞なく破棄いたします。` | 2줄로 분리 |
| 제6조 L4~L6 | 1개 단락에 `2. 個人情報の破棄手順および方法は次のとおりでございます。` + `<w:br/>` + `1) 破棄手順：…` + `<w:br/>` + `2) 破棄方法 : …` | 3줄로 분리 |

수정 커밋 이전에는 `<w:br/>` 이 파싱 단계에서 제거되어 여러 라인이 1줄로 합쳐졌음. 본 커밋 이후 개별 라인으로 분리.

## [MANUAL — 제1조 표 헤더 승격]

第1条 표의 첫 행(`区分` / `処理目的`)이 원본에서 `<td>` 일반 셀이었으나, 행 자체가 헤더 의미이므로 `<thead><tr class='table-secondary'><th>…</th></tr></thead>` 로 승격.

## [MANUAL — 표 라벨 셀 th 승격]

제11·12조 표의 좌측 라벨 셀을 `<th class='table-secondary'>`로 승격 (KO와 일관, Bootstrap 회색 배경 적용). 기존 `<thead><tr class='table-secondary'>` 음영 행은 `<tbody><tr>` + `<th class='table-secondary'>`로 변환.

| 위치 | 원본 JSON | 최종 JSON |
|------|----------|-----------|
| 第11条 표 (1행) | `<td>受付・処理部署</td>` | `<th>受付・処理部署</th>` |
| 第12条 표 — thead 첫 행 | `<thead><tr class='table-secondary'><th …>個人情報侵害申告センター…</th><th>所管業務</th><th>個人情報侵害事実申告…</th></tr></thead>` | `<tbody><tr><th rowspan='4'>…</th><th>所管業務</th><td>個人情報侵害事実申告…</td></tr>` |
| 第12条 표 — 기관명 셀 (rowspan) | `<td rowspan='…'>個人情報紛争調整委員会 / 大検察庁 / 警察庁</td>` | `<th rowspan='…'>…</th>` |
| 第12条 표 — 속성 라벨 (所管業務/ウェブサイト/電話/住所) | `<td>…</td>` | `<th>…</th>` |

## [최종 REVIEW 잔존]

`pnpm tsx scripts/verify-json.ts`:
- 번호 스타일 혼재 — 유지 정책

구조 이슈 없음.
